name: sane_pdf
description: PDF import, rendering and annotations.
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.13.0
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  sane_core:
    path: ../sane_core
dev_dependencies:
  flutter_test:
    sdk: flutter
